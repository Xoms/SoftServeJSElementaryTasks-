/*При нажатии на кнопку (например "Запустить"(кроме 3-его задания),
сработает обработчик runTask*() // где * = номер задания: 
который создаст объект состояний context и запустит последовательности функций из 
подключенного модуля task*.js. Получение и проверка данных содержаться в  

Примерная логика для каждого Task*
    Объект состояний context:
	context.params - исходные данные для каждого таска
	validateParams(context)) - проверка параметров и генерация кода ошибки в errState
	context.errState - коды ошибок;
	Tadk*.genErrMsg(context.errState) - генерирование ошибки 
	context.res - результат для выовда 
	

	
	Например:
	context.params = Task*.getParams() /*- в объект conext присваиваюццо исходные 
		данные;
		для каждого задания в своем формате, но в виде свойства одного 
		объекта contex */
	validateParams(context)) /*- Проверяет объект на ошибки ввода, соблюдение 
		условий таска,  и генерирует код ошибки, 0 - если всё ок, и возвращает
		в context.errState */

	if (context.errState){ // Ошибка есть
		errMsg(context.errState);// - генерация текста ошибки из кода ошибки
				(тут подумать)   
	
	else { 
		Task*.run; // ошибок нет, запукаем решение Task* (* = выше) вернет 
			      результат в context.result
		showRes(context); // разбереться что находить в контексте

*/
	Welcome! (SS - Guest